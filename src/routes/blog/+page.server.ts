import { posts } from './data';

export const load = async () => {
	// let summaries = posts.map((post) => ({
	// 	slug: post.slug,
	// 	title: post.title,
	// 	content: post.content
	// }));
	//
	// let summaries = posts.map((post) =>{
	// slug: post.
	// })
};
